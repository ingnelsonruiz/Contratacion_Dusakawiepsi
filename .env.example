# Proxy PostgreSQL (mismo servicio que Proyecto_Dusakawi, misma BD base_sie_dusakawi)
PROXY_URL=https://pg-proxy.onrender.com/query
PROXY_API_KEY=

# Usados solo por scripts/seed-admin.ts para crear el primer usuario admin.
# No se guardan en la BD en texto plano, solo se usan para calcular el hash SHA-256.
ADMIN_USERNAME=
ADMIN_PASSWORD=
